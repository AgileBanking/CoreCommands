package services.policies

class UsersController {

    def addUser(String personId, String user, String roles ) { 
    /*  personId    is the unique id of the person as defined in Parties
        user        is a JSON with at least the required fields
        roles       is a JSON with a collectionof roles ids assigned to the user
        
        The created user should be Inactive. The system requires a second call to Activate the user.
    */
    
    }
}
